# Extras

Optional add-ons for your Company OS. Everything in this folder is **inert until
installed** — copying a file into `.claude/` is what activates it.

The catalog with descriptions and install/remove instructions is
[`docs/addons.md`](../docs/addons.md). The `/onboard` wizard offers these at the
end of setup, and you can add or remove any of them later.
